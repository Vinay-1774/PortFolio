{
  "name": "vinay-portfolio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "python3 server.py",
    "dev:backend": "uvicorn server:app --reload --port 8000"
  },
  "devDependencies": {
    "vite": "^5.4.2"
  }
}
